//for
const numeros =[100,200,300,400,500,600]
// a primeira expressão executada apenas uma unica vez
//segunda expressão condição do código 
//a tercira expressão executada toda vez qu e executada a função
for (let indice = 0; indice < numeros.length; indice++) {
  //
  //  const element = array[index];
    console.log(numeros[indice]);
}